<p id="notice"><%= notice %></p>

<h1>Images</h1>
  <div class="flickr_pagination">
    <%= will_paginate @images, :container => false %>
  </div>
  <%= render 'images' %>
<br>

<%= link_to 'New Image', new_image_path, class: 'btn btn-outline-success' %>
